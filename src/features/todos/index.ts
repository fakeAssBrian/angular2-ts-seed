import { TodoWidgetComponent } from './components/todo-widget';
import { todoReducer } from './reducer';

export {
  TodoWidgetComponent,
  todoReducer
}

/**
 * reducers needs to be injected in the store
 */

